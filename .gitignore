# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.
# Developer note: near.gitignore will be renamed to .gitignore upon project creation
# dependencies
node_modules
/.pnp
.pnp.js
package.json
package-lock.json

# build
/out
/dist

# keys
/templates/react/neardev

# testing
/coverage
/integration-tests/target
/neardev
/integration-tests/neardev

# production
/build
/contract/target

# misc
dev.env
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local
/.cache
.idea

npm-debug.log*
yarn-debug.log*
yarn-error.log*

generated
target
.build

mutants.out*

/res/sweat_jar_main.wasm
/res/sweat_jar_commit.wasm

# Integration-test-featured contract build (make build-integration) — never commit;
# res/ holds the committed production artifacts.
/res-integration

# Replay reconciliation data (extracts + small fixtures alike) — kept locally,
# never committed. replay/tests/fixtures/ (generated at test time) is tracked.
/test_data/
/replay.db
/reconciliation.csv

# Unrelated bug-bounty working notes.
/issues
